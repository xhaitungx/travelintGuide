import React, { useState } from "react";
import { Button } from "@mui/material";

import { Process, File } from "../../components";
const Detail = () => {
  const [proccessOn, setProcessOn] = useState(true);
  return <div>{process ? <></> : <></>}</div>;
};

export default Detail;
